import BASE_COLORS from './base/colors';
import FONTS from './base/fonts';
import BASE_FONT_SIZES from './base/font-sizes';
import BASE_SPACES from './base/spaces';
import BORDER_RADIUS from './extra/border-radius';

const STYLES = {
  base: {
    colors: BASE_COLORS,
    fonts: FONTS,
    fontSizes: BASE_FONT_SIZES,
    spaces: BASE_SPACES,
  },
  extra: {
    borderRadius: BORDER_RADIUS,
  },
};

export default STYLES;
